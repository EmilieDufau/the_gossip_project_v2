<h1>Bienvenue sur mon super site de gossip !</h1>

<br>

<a href="new_gossip" class="btn btn-primary">Tu peux en créer un nouveau</a>
<br>
<br>
<h2>Jette un oeil aux potins ci-dessous :</h2>
<br>

<div class="cards">
  <% Gossip.all.each do |gossip| %>

    <div class="card m-3 border-primary rounded">
      <div class="card-body">
        <h4 class="card-title"><%= gossip.title %></h4>

        <h5 class="card-subtitle m-3 text-muted">Gossip n°<%= Gossip.all.index(gossip) + 1 %>, de <%= gossip.user.first_name %></h5>

        <a href="<%= gossip_path(gossip.id)%>" class="btn btn-primary">Lire le gossip</a>
      
      </div>
    </div>

  <% end %>

</div>

